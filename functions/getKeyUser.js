exports = async function(request){
  /*Checks if a request is from a valid user
  
  each request has to be associated with a specific user with a valid API key. This should be pased in the initial request as a header.
  This function ingests the request, compares it to existing API keys, and returns the user that key belongs to.
  
  inputs:
   - request: the request received
  output:
   - the user or null
  */
  
  
};