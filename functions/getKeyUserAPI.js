/*Sample Request (paste this in the console):
exports({query: {},headers: {"Content-Type": ["application/json"], "apiKey": "<api key>"}, body: {}})
})
*/

exports = function({ query, headers, body}, response) {
  /* wraps the getKeyUser function within an api, mostly for testing purposes. allows to see if a token is valid.
  */
  
  return response
};
